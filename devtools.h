#ifndef DEVTOOLS_H
#define DEVTOOLS_H

#include "procgen.h"

using namespace std;

// Função principal do menu de devtools
void Devtools(ConfigRNG &config);

// Função para configurar probabilidades dos desafios
void ConfigurarProbabilidades(ConfigRNG &config);

#endif

